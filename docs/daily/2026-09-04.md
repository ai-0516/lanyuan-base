# 2026-09-04 Daily Log

## 微信云托管上线排障与路线 2（callContainer 登录）实现

### 云托管服务启动成功

- 建库（`lanyuan-base`，utf8mb4_unicode_ci）后部署成功，alembic 自动建表
- 服务启动日志确认：DSH runtime、MCP session manager 均正常

### 日志可观测修复（PR #99 追加 commit 35cb551）

- **根因**：`logger.py` console handler 默认写 **stderr**，微信云托管采集路径默认只可靠采 stdout → 云端看不到应用日志
- **修复**：console handler 显式 `sys.stdout`（本地终端合并显示 stdout/stderr 造成「本地可见」假象）
- 已验证：丢 stderr 后 INFO 日志完整可见
- 采集路径：保持默认 `stdout` 即可（UI 不允许留空；stderr 非合法采集值）

### 登录 500 根因定位（POST /api/v1/auth/login → 500）

- 链路：`auth_service.login → wechat_client.code2session → api.weixin.qq.com`
- **根因**：微信云托管把 api.weixin.qq.com 流量本地代理（169.254.10.1）+ 平台自签 CA
  （`/app/cert/certificate.crt`，initenv.sh 注入）；httpx 用 certifi 证书库不认平台 CA →
  `CERTIFICATE_VERIFY_FAILED: self-signed certificate`
- 决策：不做临时 CA 信任，走**路线 2**（用户拍板）：前端 wx.cloud.callContainer + 后端
  信任平台注入的 `x-wx-openid` header → 彻底不调 api.weixin.qq.com

### Issue #100 实现（分支 feat/100-x-wx-openid）

**后端**（双路径登录，header 优先）：
- `api/v1/auth.py`：login 读 `x-wx-openid` header（平台注入）→ 传入 service
- `services/auth_service.py`：login 加 `openid` 参数——有则免 code2session
- `core/wechat.py` 安全修正：mock code2session 只属于 mock 配置（占位 appid）；
  生产真实 appid 下任何 code（含短 code/mock_code）都走真实 API——原「短 code
  启发式」会让公网请求拿到可预测 mock openid 伪造身份（公网访问暂未关闭）

**前端**（开发/线上自动分流，envVersion 判断）：
- `utils/constants.js`：`CLOUD_CONFIG = { ENV: 'test-d2gizr8ena300c58e', SERVICE: 'lanyuan-base' }`
- `utils/request.js`：develop → wx.request + localhost；trial/release →
  wx.cloud.callContainer（X-WX-SERVICE + config.env）；绝对 URL 转容器 path（保留 query）
- `app.js`：cloud 模式 wx.cloud.init
- login 页零改动（wx.login code 照发，后端 header 优先忽略之）

**测试**：+5（test_api.py header 登录/幂等/空 header 3 用例；test_wechat.py mock
语义 2 用例）→ 全量 441 passed / 4 skipped；ruff 干净；前端 node --check + mock 验证通过

**文档**：TECH_SPEC §2.4 场景 A 双路径登录 + §4.1 接口表标注

### Review 修复（PR #101 dev-lead changes_requested，2026-09-04）

dev-lead 实测抓出严重安全敞口：`x-wx-openid` 无条件信任 + 公网访问未关闭 →
HTTP 层可伪造 header 冒充任意用户建号登录（200 + JWT + 幂等复用）。修复：

- **信任门控（14:28 用户拍板删除）**：曾加 `WX_TRUST_OPENID_HEADER` 开关（默认
  false）——后端只在开关开启时才读取 `x-wx-openid`。用户 review 判定开关多余（真正
  安全边界 = 关闭公网访问，私有链路下 header 平台注入不可伪造），已删除：后端直接
  信任 header（保留格式校验 400 拒绝）
- **格式校验**：信任开启时 header openid 须匹配 `^[A-Za-z0-9_-]{1,64}$`（对齐 DB
  varchar(64)，真实微信 openid ~28 位）；非法值（超长/特殊字符）→ 40013 拒绝，不落库
  （防生产 MySQL DataError 500）
- **service 边界防御**：`auth_service.login` `if openid is None` → `if not openid`
  （空 openid 不落库）
- **前端可读错误**：`request.js` cloud 分支在 `wx.cloud`/`callContainer` 缺失时
  显式 reject（'云能力不可用：请升级微信基础库 ≥2.19.1'），替代 TypeError 空指针

测试：新增 2 用例（开关默认关闭→header 忽略走 code；开关开启→非法格式 400/合法放行）
→ 全量 **443 passed / 4 skipped**；前端 node mock 4/4（含 wx.cloud 缺失分支）；ruff 干净。
TECH_SPEC §2.4/§4.1、.env.example、daily 同步。

### 遗留（issue #100 评论注明，单列后续）

- ~~ai-chat v2 SSE 线上通道~~ → **已并入 #101 完成（见下）**
- 服务公网访问暂不关闭（用户决定）；关闭后公网无身份来源路径彻底消失

---

## v2 流式通道统一 WebSocket（并入 PR #101，用户 2026-09-04 拍板）

**决策**：不保留双模式——v2 chat 流式彻底改 WebSocket 单一通道（SSE POST /chat
删除；前端 enableChunked 解析删除）。微信云托管 callContainer 不支持流式
（enableChunked 是 callHTTPFunction 能力），WS 是云托管原生通道。

**后端**（backend/app/api/v2/ai.py）：
- `_stream_chat`（SSE 帧）→ `_chat_events`（传输无关事件生成器，yield
  `{type, data}` 白名单事件 dict）
- 删 `POST /chat`（StreamingResponse/SSE_HEADERS）；新增 `WebSocket /chat/ws`：
  一轮对话 = 一条连接；首帧 `{token, session_id, message}`（JWT 放帧内，不进
  URL）；token 无效 4401 / 归属非本人或缺失 4403 / 空参 1008——先推 error 帧
  （`{type:"error", data:{message}}`，文案后端给）再 close；正常流 turn/end 后
  close(1000)
- event_layer.format_sse 删除（孤儿）；测试 TestV2ChatOwnership 改 WS
  （TestClient websocket，真 JWT + mock 事件流，6 用例：放行/事件透传/4401/
  4403×2/1008）；test_event_layer 删 format_sse 用例
- scripts/verify_v2_m3.py：⑥ chat 归属校验 SSE HTTP → WS（websockets 17
  asyncio client）

**前端**（miniprogram）：
- ai-chat streamChat：wx.request enableChunked → WebSocket（onOpen 发首帧 →
  onMessage JSON.parse → dispatchEvent（事件协议不变，§10.1 映射表原样）
  → onClose isLoading 兜底）
- 通道（PR #101 第 3 轮 review 修正——不走云托管公网域名）：trial/release →
  **wx.cloud.connectContainer**（config.env + service + path=/api/v2/ai/chat/ws，
  与 callContainer 同一条微信私有链路：免公网域名、免 mp 后台 socket 合法域名、
  不依赖「公网访问」开关 → 「公网已关闭」信任前提可同时成立；
  基础库 ≥2.21.1，缺失 → 可读提示）；develop → wx.connectSocket
  （ws://localhost:8000，V2_BASE_URL http→ws）；CLOUD_CONFIG.HOST 删除
- 删 arrayBufferToString/TextDecoder/逐行 SSE 解析（孤儿清理）

**测试**：后端 448 passed / 4 skipped（WS 10 用例：放行/事件透传/4401/4403×2/
1008/空 token 4401/首帧超时 1008/sub 缺失或非数字 4401×2，较上轮 +4；
删 5 旧 SSE 用例）；
node mock 全链路（develop/release URL、首帧内容、事件分发渲染、error 帧文案、
非 JSON 帧忽略、onClose 兜底）全过

**文档**：docs/dsh/TECH_SPEC.md（§1.2 架构 / §3.3 数据流 / §4.3 帧化 / §6.1 /
§9.1 路径 + 归属校验处）；daily 本段

**上线前提**（待真机验证）：云托管支持 WebSocket（connectContainer 走微信私有
链路，无需 mp 后台配 socket 合法域名；基础库 ≥2.21.1）；体验版真机实测
connectContainer 事件流到达 + 云托管网关 WebSocket 行为
