# 2026-07-30：Error Recovery — 三层容错

## 今日成果 🎯
- 完成 Issue #10 Error Recovery 全栈实现（PR #12）
- 新增 `errors.py`（11 个错误码 + 恢复策略表 + 退避算法）
- 重构 `streaming.py`（错误分类 + 重试包装 + SSE 断流检测 + 重大日志）
- 17 个测试，全量 76 测试通过

**独立产出：** [error-recovery.md](../error-recovery.md) — 完整架构说明

## 核心决策 🔑
1. 三层恢复：退避重试 → 降级模拟回复 → 绝对兜底
2. 错误码用 Enum + 策略表（`RETRY_CONFIG`），不要散落字符串
3. 重大日志在源文件记，不走 hook
4. SSE 断流先不续流，记日志收集数据

## 关键讨论 💬
- 全链路错误分类 → 发现 5 个盲区（SSE 断流等）
- 日志归属争议 → 结论：hook 适合结构化消费，不适合异常排查

## Review 流程固化 ✅
- **B 维度审查方式更新**：从纯代码审查改为"实际触发验证"，不只看代码逻辑，用 mock script 真实跑 7 种错误场景验证行为
- **新增验证工具**：`backend/scripts/review_llm_error_recovery.py` — 可复用的 mock 触发脚本，支持所有错误码分支验证
- **Review 发现 3 个问题**：格式不一致（AUTH_FAILED 空格）、无效重试（PAYLOAD_TOO_LARGE #8 前提）、注释矛盾（SSE_DISCONNECTED doc vs config）→ inline comments 全部修复
- **PR #14 完整流程**：Issue #13 → Worktree → 提交 → PR → Lint 修复 → Merge → 清理，走通了完整闭环
