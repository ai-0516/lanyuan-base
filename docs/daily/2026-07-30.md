# 2026-07-30：Error Recovery — 三层容错

## 今日成果(Dev) 🎯
- **完成 Issue #10 Error Recovery 全栈实现**（PR #12）— 两轮 review，13 条 inline comments 全部修复
- 新增 `errors.py`（11 个错误码 + 恢复策略表 + 指数退避算法）
- 重构 `streaming.py`（错误分类 + 重试包装 + SSE 断流检测 + 重大日志）
- 重构 `agent.py`（错误处理从 agent 层移入 streaming 层 → `fallback` 事件）
- 17 个测试，全量通过

**独立产出：** [error-recovery.md](../error-recovery.md) — 完整架构说明

## 今日成果(Reviewer) 📋

### 1. PR #12 Review：LLM 错误恢复

**初轮 review：** 跑 UT、读代码，提了 3 个 inline comments + 六维度 summary
- `AUTH_FAILED` 格式空格
- `PAYLOAD_TOO_LARGE` 无效重试（无压缩逻辑）
- `SSE_DISCONNECTED` 注释矛盾

**教训：** 应该实际触发验证，而不是只读代码 + 跑 UT。

**补充验证：** 写了 `review_llm_error_recovery.py`，用 `unittest.mock` 模拟 7 种场景（429/401/413/400/529 + SSE 断流 + SSE 解析失败），全部实际触发确认行为和日志。

**修复后再审：** 3 个问题全部确认修复 ✅
- 额外发现 Retry-After 响应头未传递（小问题）

**Dev 提审 comments（最有价值的部分）：**
- `base_delay_ms` 无防御 → 会 KeyError
- magic number 10 未命名
- messages 是否会被篡改
- 未知状态码缺 log
- chunk 截断 500 字节
- fallback 逻辑职责分离
- max_retries 硬编码

### 2. 流程优化
- **PR #14** — 将 `review_llm_error_recovery.py` 通过完整 Issue→Branch→Worktree→PR→Merge 流程入库
- **PR #14 Lint 修复** — CI 报 F541 f-string 无占位符，6 处自动修复
- **Skill 市场调研** — 搜索了 hermes skills search，确认市场上没有我们需要的防御性编码细项 checklist

### 3. Review 方法论固化

**code-review skill 重写：**
- Bug Detection：从 5 条笼统项 → 7 条具体项（含 KeyError 风险、函数副作用、条件写反、边界遗漏）
- Code Quality：从 5 条笼统项 → 8 组可执行检查项（防御取值、magic number、错误日志完整性、可配置性、边界条件等）

**github-workflow skill 更新：**
- B 维度：每个分支实际触发验证
- C 维度：引用 code-review skill 的详细 checklist

### 4. 核心认知
> "对已经有的部分提 comment 是容易的，对应该有却没有的内容提 comment 是不容易的"
>
> Dev review 时是在拿「理想代码模型」和实际做 diff，Reviewer 看到的是眼前的代码。细化 checklist 就是在脑子里逐步建立这个理想模型。

## 第二波改动（Review 后）🔧

| 改动 | 原因 |
|------|------|
| `base_delay_ms` 默认 fallback | review：`config["base_delay_ms"]` 可能 KeyError |
| 未知 HTTP code 加 log | review：UNEXPECTED 会丢失原始状态码 |
| 不截断 last_error_chunk | review：500 字符影响复现 |
| magic number 10 → 动态计算 | review：硬编码不可维护 |
| `max_retries` 默认 fallback | review：与 base_delay_ms 同理 |
| `RETRY_EXHAUSTED` → `fallback` 事件 | review：错误处理集中到 streaming.py |
| 首次尝试直接 yield（流式） | review：缓存所有尝试失去流式意义 |
| 新增 `retrying` 事件 + 俏皮文案 | UX：重试期间给前端状态提示 |

## 核心决策 🔑
1. 三层恢复：退避重试 → 降级模拟回复 → 绝对兜底
2. 错误码用 Enum + 策略表（`RETRY_CONFIG`），不要散落字符串
3. 重大日志在源文件记，不走 hook
4. SSE 断流先不续流，记日志收集数据
5. 首次尝试直接流式，重试才缓存（保证正常情况体验）
6. `fallback` 事件代替 `RETRY_EXHAUSTED` 错误码，明确语义分层

## 关键讨论 💬
- 全链路错误分类 → 发现 5 个盲区（SSE 断流等）
- 日志归属争议 → 结论：hook 适合结构化消费，不适合异常排查
- PAYLOAD_TOO_LARGE 无 #8 压缩 → 当前不重试，改为 None
- SSE_DISCONNECTED doc 矛盾 → doc 更新为可重试

## Review 流程固化 ✅
- **B 维度审查方式更新**：从纯代码审查改为"实际触发验证"，用 mock script 真实跑 7 种错误场景
- **新增验证工具**：`backend/scripts/review_llm_error_recovery.py` — 可复用的 mock 触发脚本
- **PR #14 完整流程**：Issue #13 → Worktree → 提交 → PR → Lint 修复 → Merge → 清理
- **两轮 review 模式**：inline comments → fix → re-review → inline comments → fix → re-review → ✅ approve

## CI 配置改动 📋
- 移除 `paths:` 过滤 → 全仓库触发（3 分钟练确定性）
- 新增 `changes` job 后改为全量触发（避免 docs-only 掩盖前次失败）
- coverage comment 删旧重建，始终在 PR 最底部

## 技术债 📦
- [ ] 给 dev 和 dev-lead 配置不同的 GitHub 账户，避免 credentials 混淆
