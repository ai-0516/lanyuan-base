# 2026-08-02 — 统一错误格式（#27）+ Like 外键约束（#28）Review

> 分 dev / dev-lead 两部分，各自填写各自的总结（沿用 08-01 起的格式）。

# Dev — 实现总结

### 成果

1. **Issue #27 统一错误响应格式**（PR #31）：
   - 根因：Starlette 对 `HTTPException` 是**精确键查找**，只注册 `Exception` 处理器时 401/404/422 走默认格式（顶层无 `code`），前端统一错误处理失效
   - 修复：注册 `StarletteHTTPException` + `RequestValidationError` 处理器；`api_exception_handler` 对无 detail 的 404 补默认文案（原来 `str(None)` → "None"）
   - **实测发现 FastAPI 0.141.1 重构**：`RequestValidationError` 已不是 HTTPException 子类（MRO: `RequestValidationError → ValidationException → Exception`，无 `detail`/`status_code`），不能复用 `api_exception_handler`，新增专用 `validation_exception_handler`（取首条错误的 `msg` 作为 message）
   - 修复后 401（40100）/ 404（40400）/ 422（42200）/ 业务错误均为 `{code, message}` 统一格式
2. **Issue #28 Like 外键约束**：
   - 模型加 `ForeignKey`（`posts.id` / `users.id`，`ondelete="CASCADE"`）；SQLite 显式开 `PRAGMA foreign_keys=ON`（SQLite 默认不强制外键）
   - 服务层 `like_post` 先校验帖子存在，不存在返回 `(None, 0)` → API 层 `40401 "帖子不存在"`（业务失败≠系统异常，避免 FK 抛 IntegrityError → 500）
   - 迁移 `28d5e00fd033`：先清存量孤儿数据再按方言建 FK（SQLite `batch_alter_table` / MySQL `create_foreign_key`）
3. **测试**：新增 7 个（服务层：FK 拒绝孤儿行 / 级联删除 / 点赞不存在帖子；API 层：401/404/422 统一格式 + 点赞业务错误），更新 e2e 旧断言（原断言依赖无 FK 的 bug 行为）；`pytest` 170 passed，ruff 通过
4. **流程**：`dev-team` skill disable（kanban 流程废弃，统一走 GitHub Issue→Branch→PR 流程）；doc 规则更新（merge 前强制 → dev 自定时机但必须写）

### 决策记录

1. **40100 语义**：无 token 走 HTTPBearer 默认 401（detail 为字符串 "Not authenticated"），统一后 `code=40100`；带无效 token 的 40101/40102 保持原样（TECH_SPEC 错误码表补 40100 行）
2. **服务层校验优先于 DB 约束兜底**：`like_post` 先查帖子存在性返回业务错误，DB FK 是最后防线（防直接 SQL / 绕过服务层的调用）
3. **迁移双方言**：SQLite 不支持 `ALTER TABLE ADD CONSTRAINT` 需 batch 重建；MySQL 直接 `create_foreign_key`；孤儿数据先清否则 MySQL 建 FK 失败

### 教训

- **config.yaml symlink 坑（第二次发生）**：Hermes `save_config` 用原子写（临时文件 + rename）会把 symlink **替换成独立文件**。配置真身在 `workspace/my-hermes/`（git 仓库），profile 目录下是指针；改配置后必须 `ls -la` 检查并恢复
- **re-dump 污染**：`save_config` 重写会改键顺序、可能污染部分值（本次 `max_iterations`/`tool_preview_length` 曾与真源不一致），以 `workspace/my-hermes` 真源为准恢复
- **验证迁移不要在 SQLite 上跑全链**：旧迁移 `f468c671cbff`（avatar TEXT）是 MySQL 语法 `ALTER COLUMN`，SQLite 会挂；应单独 stamp + 手工建旧结构验证新迁移

### Issue #9 跨会话记忆（Memory）实现总结

### 背景与设计（先讨论后动手）

与用户讨论确认的架构决策：
- **Provider 抽象层**：`MemoryProvider` ABC 定义「写入 / 读取 / 抽取」三组基本操作，`MySQLMemoryProvider` 是首个实现（数据存 DB）；未来可加云 API provider（云端存储/读取/抽取），`get_provider()` 为全局切换点
- **提取时机**：`agent:end` 事件异步提取，每轮 LLM 判断（召回全、不阻塞 SSE 流）；hook 内独立 DB session（仿 stats.py），失败只记日志
- **相关记忆选择**：LLM side-query 选相关（用户倾向）→ 失败降级关键词匹配（provider.search）
- **记忆类型**：社区助手场景只需 2 类（user=偏好/身份、reference=关注事项），保留 type 字段让 LLM 分类；feedback 类以后可能演进为 skill（暂不做）
- **上限与合并**：每用户 30 条（MEMORY_MAX_PER_USER），写入超限时被动触发 LLM 合并去重（低频）；记忆带 created_at/updated_at，LLM 可判断新旧覆盖（如搬家场景）
- **API**：@tool + REST 合一（GET/POST/DELETE /memory），既给 LLM 提供 memory_list/add/delete 工具，也方便未来前端管理

### 实现

```
backend/app/
├── models/user_memory.py              # UserMemory 模型（id, user_id, name, type, description, body, created_at, updated_at）
├── alembic/versions/3f9b1c2d4e5a      # add user_memories 表迁移
├── harness/memory.py                  # MemoryProvider ABC + MySQLMemoryProvider
│     - add（超限先 consolidate）/ delete / list_all / search（关键词打分）/ extract / consolidate
├── harness/memory_select.py           # LLM 选相关 + 关键词降级
├── harness/hooks/memory_extract.py    # agent:end 异步提取（按 session 过滤，防跨用户串味）
├── harness/context.py                 # build_memory_index + build_relevant_section + 注入 SYSTEM
├── api/v1/memory.py                   # @tool：memory_list / memory_add / memory_delete + REST
├── services/ai_service.py             # stream_chat 加载索引 + 选相关注入
└── config.py                          # MEMORY_MAX_PER_USER=30 / MEMORY_INDEX_LIMIT=30
```

### 测试

新增 17 个：provider 存储（add/类型回退/超限合并/仍满抛错）、检索（倒序/隔离/仅删本人/关键词打分）、上下文注入（索引/相关段/空）、API（CRUD/非法 type/用户隔离/跨用户删除）。
`pytest` 187 passed, 4 skipped（原 170 + 新 17）；test_hooks 计数 22→24（agent:start/end 各 +1）。

### 决策记录

1. **类方法名不能用 `list`**：类内 `async def list()` 会 shadow 内建 `list`，导致其他方法注解 `list[str]` 解析失败（`function object is not subscriptable`）→ 命名 `list_all`
2. **提取输入按 session_id 过滤**：初始实现未过滤会读全部用户消息（隐私泄漏 + 串味），改为 `where(Message.conversation_id == session_id)`
3. **注入放 SYSTEM 而非 user turn**：与 s09 不同（s09 注入 user turn），我们拼进 system prompt，避免破坏 role 交替

# Dev-Lead — Review 总结

### PR 审查记录

**PR #31（fix: 统一错误响应格式 #27 + Like 外键约束 #28）— Approve ✅**

- 审查维度：A 安全（无密钥、无注入，FK/清理均为参数化 DDL）/ B 业务正确（**12 项实测触发验证全过**）/ C 代码质量（良好，2 个建议）/ D 测试（新增 7 个 + e2e 更新）/ E 设计稿（后端 PR 不涉及）/ F 性能（无 N+1，like_post 增加 1 次帖子查询可接受）
- 实测记录（`uv run python scripts/review/error_format_like_fk/review.py`，本次 review 沉淀入库）：
  - #27 统一格式 4 场景：401 → 40100、404 → 40400 Not Found（默认文案，原为 "None"）、422 → 42200 Field required（专用 handler）、40101 业务 dict detail 透传
  - #28 Like FK 5 场景：点赞不存在帖子 → 400/40401（非 500）、真实点赞 → 200、幂等取消、ORM 直插孤儿行 → IntegrityError（PRAGMA per-connection 生效）、删帖 → likes 级联清除
  - 迁移 28d5e00fd033 3 项（SQLite 增量）：孤儿清理（合法保留/孤儿清除）、双 FK + CASCADE 建立、downgrade 对称回滚
  - 基线：pytest 170 passed, 4 skipped；ruff 通过
- 建议（不阻塞）：① PR body「SQLite upgrade 全通过」不准确（卡在既有 avatar 迁移）→ 另开 Issue；② like_post TOCTOU 竞态可选改进（存在性检查后 flush 前删帖 → 500，概率极低，DB 约束兜底）
- 正面反馈：根因分析精准（Starlette 精确键查找 + RequestValidationError 0.141+ 非 HTTPException 子类，断言实测成立）、迁移脚本方言分支处理得当、SQLite PRAGMA 事件监听细节到位、测试精准命中验收标准（e2e 旧断言正确更新——原断言依赖无 FK 的 bug 行为）

**PR #33（feat: 跨会话记忆系统 #9）— Approve ✅（两轮）**

- 审查维度：A 安全（JWT 强制认证、user_id 隔离实测通过、无注入）/ B 业务正确（**18 项实测触发验证全过**）/ C 代码质量（架构清晰，4 个建议→全部采纳）/ D 测试（新增 17 + 修复后补 14 个，201 passed）/ E 设计稿（后端 PR 不涉及）/ F 性能（每轮 LLM side-query 成本点 → 用户拍板方案 b 解决）
- 实测记录（`uv run python scripts/review/memory_system/review.py`，本次 review 沉淀入库）：
  - CRUD API 4 项：添加/列表/非法 type→40011/删除 ✅
  - 用户隔离 2 项：A 见 1 条 B 见 0 条、B 删 A 记忆失败 ✅
  - 记忆注入 3 项：build_memory_index / build_relevant_section / build_deepseek_messages system 含索引+body ✅
  - @tool 注册：memory_list/add/delete 共 18 tools ✅
  - memory_select 策略 5 项：关键词命中零 LLM、无命中 LLM 兜底、LLM 抛错返回 [] 不阻塞、无记忆短路、中文 bigram 提取 ✅
  - consolidate 保护 2 项：LLM 返回全无效保留原数据、body 截断 2000 ✅
  - hook 层 extract 失败不阻塞（on_agent_end try/except）✅
- 建议（4 个，全部被 dev 采纳并二次验证）：① 每轮 LLM side-query 成本 → 方案 b 关键词优先+LLM 兜底（用户拍板，首 token 延时优先）；② consolidate 全删重写丢数据风险 → 预解析过滤非法项 + body 截断 2000；③ daily 格式偏离 → 归入 Dev 部分一级标题下；④ _llm_select 缺单测 → 补 6 个
- 正面反馈：MemoryProvider ABC 抽象层 + get_provider() 切换点为云 API provider 留扩展位、req_id 关联保证并发请求不串味、hook 内独立 DB session + 异常只记日志不影响 SSE 流、防御性细节到位（consolidate 空返回不误删/无记忆短路/type fallback/_parse_json_array 容忍 markdown）
