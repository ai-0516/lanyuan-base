# 2026-08-31 — v2 M2 review 修复：/mcp 内部共享密钥认证（PR #94 第二轮）

# Dev — 实现总结

## 背景

devlead review PR #94（v2 M2 工具桥）返回 **Request Changes**，A 维度严重安全问题：

> `/mcp` 端点无认证 + `_meta.user_id` 可伪造 → 任意网络可达者可冒充任意用户调用
> 全部 19 个业务工具（实测：伪造 `_meta.user_id=777` 修改 victim 昵称为 HACKED、
> 以 victim 身份发帖）。`_meta` 是 MCP 协议中 client 自由填写的字段，桥层注入只是
> 约定不是强制。

另有 2 条不阻塞建议：`LANYUAN_MCP_URL` 默认值收敛为可注入常量；`_to_dict` 与 v1
内联等价（已注释「不 import v1」合理，低优先不改）。

## 修复方案（review 建议方向一：内部共享密钥）

| 文件 | 改动 |
|---|---|
| `backend/app/core/security.py` | 新增 MCP 内部认证：`MCP_AUTH_HEADER` / `get_mcp_token()`（显式 env `LANYUAN_MCP_TOKEN` 优先，未配置进程内自动生成随机密钥，跨线程安全）/ `verify_mcp_token()`（hmac 常量时间比较） |
| `backend/tools/mcp_server/security.py` | **新建** `McpAuthMiddleware`（纯 ASGI）：校验 `X-Lanyuan-Internal-Token`，失败 401（JSON code=401）——所有请求含 GET 初始化在 streamable-http 路由前拒绝 |
| `backend/tools/mcp_server/main.py` | `mcp.http_app(middleware=[Middleware(McpAuthMiddleware)])` |
| `backend/app/ai/dsh_runtime.py` | `_runtime_env()` 注入 `LANYUAN_MCP_TOKEN = get_mcp_token()`（与 FastAPI 进程同值，桥凭此过认证）；`LANYUAN_MCP_URL` 默认值收敛为常量 `LANYUAN_MCP_URL_DEFAULT` + 部署注释（回应建议 1） |
| `dsh/mcp-client/src/index.ts` | `connectWithRetry` 带 `requestInit.headers`；`apply()` 读 `process.env.LANUAN_MCP_TOKEN`，缺失 → 抛错拒绝连接（fail-closed） |
| `dsh/cordis-lanyuan.yml` | mcp-client 条目注释 token env |
| `backend/tests/test_mcp_server.py` | 新增 9 个测试：无凭证/伪凭证 401、GET 初始化 401、正确密钥完整握手 200、token 自动生成/env 优先/常量比较、runtime env 注入同值、URL 常量 |
| `backend/tools/mcp_server/decorator.py` | `_classify` 签名换行（存量 E501，CI lint 红，顺手修复） |
| `docs/dsh/TECH_SPEC.md` | §6.1 认证 bullet、§6.2 token 配置、§6.3 信任前提收紧、§12.2 安全条目 |

## 关键设计

- **信任前提收紧**：`_meta.user_id` 只在「已通过传输层内部认证的 client」上采信。
  唯一持有密钥的是本进程 DSH 子进程（桥），外部 client 直连 /mcp 在传输层即 401
  ——「能到达工具的 client 只有桥」+「桥注入值来自 session id 非模型输入」两层
  构成身份边界。
- **默认安全（fail-closed）**：token 未显式配置时进程内自动生成（`secrets.token_urlsafe(32)`），
  dsh_runtime 注入同值到 DSH 子进程 env——开发零配置，外部无法猜测；生产可显式
  固定 env `LANYUAN_MCP_TOKEN`（FastAPI 与 DSH 共享）。
- **不阻塞建议处理**：URL 常量收敛 + 注释（建议 1 低成本落地）；`_to_dict` 保持
  v1 内联等价（建议 2 注明低优先，v1 退役后再收敛公共 utils）。

## 验证

- 定向：`test_mcp_server.py` **28 passed**（原 19 + 新增 9）。
- 全量：**412 passed, 4 skipped**（原 403 + 9）。
- ruff：`All checks passed`（含存量 E501 修复）。
- TS build：`pnpm build` 通过。
- 复现路径闭环：无凭证 POST /mcp tools/list → **401**（此前 200 裸奔）；伪造
  token → 401；正确密钥完整握手 → tools/list 200。

---

# 2026-08-31（续）— v2 M3：会话（MySQL PersistenceBackend + get-or-load-or-create）

# Dev — 实现总结

## 背景

issue #90（v2 M3，TECH_SPEC §5/§8.2）：过渡期「每请求新 session（uuid，无注入）」
退役——MySQL 持久化 + get-or-load-or-create 恢复，配套使用（持久化提供 load
数据源，恢复提供 load 逻辑）。

## 实现

| 文件 | 改动 |
|---|---|
| `dsh/mysql-persistence/`（新建，`@lanyuan/dsh-session-persistence-mysql`） | TS 插件：PersistenceCoordinator 编排层全复用（官方包），物理层 8 hook 只写 MySQL store（照官方 SqliteStore 翻译）——`appendBatch` InnoDB 事务 + `SELECT ... FOR UPDATE` 锁行读 tail → 校验 `first.seq === next seq` → 批量 INSERT → revision+1；`loadStoredFrom`（`WHERE seq >= ?`）；TornMarker 用 number；不做 chunk 打包 codec；幂等建三表（sessions/events/persistence_state，§8.2 含 owner_user_id） |
| `dsh/server/`（新建，`@lanyuan/dsh-server`） | 本地 JSON-RPC server 替换官方 sdk-jsonrpc-server：`getOrCreateSession` 扩展为 get-or-load-or-create——内存有？用内存的（session 亲和）；持久化有？`agents.resume`（log-seed 重放，官方 `restoreOrCreateConfigured` 同款模式）；都没有？`agents.create`。collision 守卫：load 失败且 id 存在 → 抛错不静默新建。裁剪 subagent 通知（lanyuan 不启用 subagent） |
| `backend/app/ai/session_service.py`（新建） | session id 生成（`v2-{纯 uuid}`）+ owner 映射写 MySQL sessions 表（`INSERT..ON DUPLICATE KEY UPDATE`，幂等）+ 幂等建表兜底 |
| `backend/app/api/internal.py`（新建） | `GET /api/internal/sessions/{id}/owner`（X-Lanyuan-Internal-Token 防护，与 /mcp 同密钥；404 fail-closed）——§6.3 身份查询插件 |
| `backend/app/api/v2/ai.py` | ChatRequestV2 加 `session_id`（复用/恢复）；无则生成纯 uuid；每次请求 upsert owner 映射；session id 经响应头 `X-Session-Id` 回传（事件流保持 DSH 原样） |
| `backend/app/ai/dsh_runtime.py` | `_runtime_env` 注入 `LANYUAN_MYSQL_*`（从 DATABASE_URL 推导，同库同凭据） |
| `dsh/mcp-client/src/index.ts` | §6.3 M3 演进：删 session id 编码解析，execute 时查询内部身份端点（纯 uuid → owner_user_id → `_meta`），查不到拒绝（fail-closed） |
| `dsh/cordis-lanyuan.yml` | jsonl persistence `disabled: true`；mysql-persistence 激活（env 引用，port 显式 `Number()`）；sdk-jsonrpc-server → `@lanyuan/dsh-server` |
| `dsh/package.json` / `pnpm-workspace.yaml` | 新本地插件入 workspace；esbuild 允许（后因弃 vitest 移除） |
| `backend/tests/test_internal_api.py`（新建） | internal 端点 4 测试（鉴权 401 / 200 / 404，mock DB） |
| `backend/scripts/verify_v2_m3.py`（新建） | 集成验证（见下） |

## 关键设计

- **两层架构（§5.2）**：PersistenceCoordinator（官方）全复用 + 8 hook 只写 MySQL——
  编排逻辑（seq 校验/contiguity/准备/修复）零重写，物理层对齐 SqliteStore 翻译。
- **get-or-load-or-create（§5.3）**：官方 rc.5 缺口（`getOrCreateSession` 只查内存）——
  服务端策略扩展，Python SDK 零改动；id 即身份（无条件 load）。
- **身份权威收归 FastAPI（§6.3 M3）**：session_id 纯 uuid（编码方案退役）；
  owner 映射 = sessions 表 owner_user_id；内部端点 + 桥插件查询（fail-closed）。
- **两写入方收敛**：FastAPI upsert owner（不碰 header 列）与 DSH 插件 header upsert
  （不碰 owner/incarnation/revision）互不覆盖。

## 验证

- mysql-persistence 单测（node --test + 真实 MySQL lanyuan_test）：**11/11 通过**
  （appendBatch 事务/revision 递增/seq 不连续拒写/surface 字段往返/loadStoredFrom/
  revision 格式/commitRepair/list/schema 编解码）。
- backend 全量：**416 passed, 4 skipped**（原 412 + internal 4）。
- TS build：4 插件（spine/mcp-client/mysql-persistence/server）全过；ruff 全过。
- `verify_v2_m3.py` 集成（真实 MySQL + 真实对话，FastAPI 同 loop 承载 /mcp）：
  同 session 复用 turn 1→2 ✓；close runtime 重启后同一 session id 恢复（上下文
  含首轮「验证用户」）✓；MySQL 落库（revision=35、events 643 条、owner 正确）✓；
  身份查询链路（get_my_profile 经内部端点注入 `_meta.user_id=1`）✓。

## 遗留（不阻塞 M3，建议单独 issue）

- **fastmcp 工具执行环境与 SQLAlchemy async greenlet 的兼容问题**：MCP 模式写工具
  （memory_add 实测）在 MySQL 下报 MissingGreenlet（pre_ping/cursor 的
  `await_only` 无 greenlet 上下文；M2 verify 用 SQLite 未暴露——SQLite 的
  implicit returning 回填 server_default 列，且连接无 pre_ping）。读工具
  （get_my_profile）正常。已定位到 fastmcp 执行链（call_tool → dependencies），
  修复需单独 issue（fastmcp/anyio/SQLAlchemy 三方上下文分析）。
- 期间误写 main 仓库 decorator.py（_refresh_expired 实验）——已 `git checkout --`
  回退并确认 main 干净（铁律重申：write_file/patch 只用 worktree 绝对路径）。
