# 2026-08-23 工作日志

## Dev Lead — v2 M1（PR #92）review

### 审查对象

PR #92（v2 M1 骨架：dsh/ 家目录 + 自写 spine/bin + 事件层 + /api/v2/ai/chat），Request Changes。

### Review 检查清单

| # | 检查项 | 步骤 | 期望结果 |
|---|--------|------|----------|
| 1 | 事件层白名单过滤（text-delta 放行）| `should_forward({"type":"assistant/chunk","data":{"chunk":{"type":"text-delta","text":"你好"}}})` | True |
| 2 | 事件层过滤（reasoning-delta）| `should_forward(assistant/chunk + reasoning-delta)` | False |
| 3 | 事件层过滤（chunk 其他子类型）| block-start/block-end/usage/finish 逐个 | False |
| 4 | 事件层放行（step/start、user/message、turn/start、turn/end）| 各事件构造 | True |
| 5 | 事件层过滤（tool/*、内部事件）| tool/call、tool/result、session/title、request/header、agent/inbox/spliced | False |
| 6 | SSE 帧格式 | `format_sse` 输出 | `event: <type>\ndata: <json>\n\n`，type+data 原样 |
| 7 | SSE unicode | format_sse 中文 | 中文不转义保留 |
| 8 | done 判定 | turn/end 事件 | is_done_event True |
| 9 | API 路由 | /api/v2/ai/chat | router prefix /ai + 挂 /api/v2、SSE_HEADERS、get_current_user 依赖 |
| 10 | 空消息校验 | message="" | 422 |
| 11 | DSH_DIR 路径 | parents[3] | 仓库根/dsh，cordis/bin 存在 |
| 12 | 环境变量管理 | _runtime_env() | DSH_CORDIS_CONFIG/DSH_HOME/DSH_SESSION_ROOT 显式设置 |
| 13 | 崩溃恢复 | DshRuntime.run 源码 | 含 close()+start() 重试 |
| 14 | 全量测试（有 SDK venv）| pytest tests/ | 384 passed, 4 skipped |
| 15 | 干净环境测试（无 SDK venv）| pytest tests/ | ❌ 集合失败（ModuleNotFoundError: deepseek_harness）——**严重问题** |
| 16 | pnpm lockfile | pnpm install --lockfile-only --dry-run | 通过（518 包，lockfile up to date）|

### 严重问题（必须修复）

1. **deepseek-harness-sdk 依赖未声明**（pyproject.toml / uv.lock 均无）——干净环境 `import app.main` 崩，v1 测试保护丢失（实测无 SDK venv 集合失败）

### 建议改进

- spine 编译无自动化（无 postinstall/prepare）
- main.py lifespan 未按 TECH_SPEC §3.1 预热 runtime（懒启动）
- pnpm-workspace.yaml allowBuilds 占位符无效值

### 关键洞察

- v2 M1 骨架架构正确（spine 裁剪、bin 自写、事件层过滤、api 隔离均对齐 TECH_SPEC）
- SDK 依赖缺失是「测试通过 ≠ 生产路径」的又一实例（dev 本地手动装了 SDK 所以全绿，干净环境崩）——#39/#69 同源教训
